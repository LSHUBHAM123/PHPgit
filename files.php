<?php

// $a = readfile("myfile.txt");     //will return file characters not count

// echo $a;                 //readfile when given as assignment value,THEN
                            //it reads file and also returns number of character read if echo is done

// readfile("myfile.txt");   //it will directly return the text without counting the characters

// readfile("MySql.php");  //can read any file and throws/outputs in output buffer
readfile("file.html");
?>